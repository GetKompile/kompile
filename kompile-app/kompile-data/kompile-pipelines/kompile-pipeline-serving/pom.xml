<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ai.kompile</groupId>
        <artifactId>kompile-pipelines</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>kompile-pipeline-serving</artifactId>
    <name>Kompile Pipeline Serving</name>
    <description>Subprocess-based pipeline execution and serving with memory isolation, health monitoring, and OOM recovery</description>

    <dependencies>
        <!-- Kompile core subprocess infrastructure -->
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-app-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-utils</artifactId>
        </dependency>

        <!-- Kompile CLI Common (KompileHome, SubprocessLogWriter) -->
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-cli-common</artifactId>
        </dependency>

        <!-- Pipeline Framework -->
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-pipelines-framework-api</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-pipelines-framework-core</artifactId>
        </dependency>
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-pipelines-framework-runtime</artifactId>
        </dependency>

        <!-- The standalone runtime owns ND4J initialization. Resolve the Java backend and
             its classified native payload from the distribution's canonical backend properties. -->
        <dependency>
            <groupId>org.eclipse.deeplearning4j</groupId>
            <artifactId>${nd4j.backend}</artifactId>
            <version>${nd4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.deeplearning4j</groupId>
            <artifactId>${nd4j.native.backend}</artifactId>
            <version>${nd4j.version}</version>
            <classifier>${javacpp.platform}${javacpp.platform.extension}</classifier>
        </dependency>

        <!-- Runtime step plugins are packaged into the standalone runtime but are not
             propagated to MCP/CLI parents that depend on this launcher API. -->
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-pipelines-steps-vlm</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>ai.kompile</groupId>
            <artifactId>kompile-pipelines-steps-samediff</artifactId>
            <version>${project.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot (optional - for launcher integration) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-yaml</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Jakarta -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
        </dependency>

        <!-- SLF4J -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- SameDiff importer hooks are discovered through ClassGraph on the JVM. The
                 native pipeline runtime carries the baked scan once for every model step. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-onnx-importer-classgraph-scan</id>
                        <phase>generate-resources</phase>
                        <goals><goal>unpack</goal></goals>
                        <configuration>
                            <outputDirectory>${project.build.outputDirectory}</outputDirectory>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.eclipse.deeplearning4j</groupId>
                                    <artifactId>sdx-aot</artifactId>
                                    <version>${nd4j.version}</version>
                                    <type>jar</type>
                                    <overWrite>true</overWrite>
                                    <includes>sdx-classgraph-scan.json</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>exec</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>ai.kompile.pipeline.serving.subprocess.PipelineServingSubprocessMain</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>native</id>
            <activation>
                <property>
                    <name>kompile.dist</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <!-- Each native worker owns its side-loaded dependency payload. -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>unpack-native-libs</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>unpack-dependencies</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/native-libs</outputDirectory>
                                    <includeTypes>jar</includeTypes>
                                    <includes>**/*.so,**/*.so.*,**/*.dylib,**/*.dll,**/shared-runtime-manifest.txt</includes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>0.10.1</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>build-native-pipeline-serving</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>compile-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <mainClass>ai.kompile.pipeline.serving.subprocess.PipelineServingSubprocessMain</mainClass>
                            <imageName>kompile-pipeline-serving</imageName>
                            <fallback>false</fallback>
                            <quickBuild>${native.quickBuild}</quickBuild>
                            <metadataRepository>
                                <enabled>true</enabled>
                            </metadataRepository>
                            <buildArgs combine.children="append">
                                <buildArg>--no-fallback</buildArg>
                                <buildArg>--enable-url-protocols=http,https</buildArg>
                                <buildArg>-H:+AddAllCharsets</buildArg>
                                <buildArg>-H:ConfigurationFileDirectories=${project.basedir}/../../../kompile-app-parent/kompile-app-main/src/main/resources/META-INF/native-image/pipeline-runtime</buildArg>
                                <!-- MarkerFactory initializes the selected logging provider;
                                     keep that complete initialization at runtime. -->
                                <buildArg>--initialize-at-run-time=org.slf4j,ch.qos.logback</buildArg>
                                <buildArg>--initialize-at-run-time=org.bytedeco.javacpp,org.nd4j,org.eclipse.deeplearning4j</buildArg>
                                <buildArg>-H:IncludeResources=${native.bytedeco.metadata.pattern}</buildArg>
                                <buildArg>-H:IncludeResources=${native.nd4j.config.pattern}</buildArg>
                                <buildArg>-H:ExcludeResources=${native.exclude.platform.payload}</buildArg>
                                <buildArg>-H:ExcludeResources=${native.exclude.library.payload}</buildArg>
                            </buildArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- Match app-main's CUDA activation and platform redist lane; the
             standalone worker must not depend on app-main or a host toolkit. -->
        <profile>
            <id>cuda-dual-backend</id>
            <activation>
                <property>
                    <name>kompile.cuda</name>
                </property>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.bytedeco</groupId>
                    <artifactId>cuda-redist</artifactId>
                    <version>${kompile.cuda.redist.version}</version>
                    <classifier>${javacpp.platform}</classifier>
                </dependency>
                <dependency>
                    <groupId>org.bytedeco</groupId>
                    <artifactId>cuda-redist-cublas</artifactId>
                    <version>${kompile.cuda.redist.version}</version>
                    <classifier>${javacpp.platform}</classifier>
                </dependency>
                <dependency>
                    <groupId>org.bytedeco</groupId>
                    <artifactId>cuda-redist-cusolver</artifactId>
                    <version>${kompile.cuda.redist.version}</version>
                    <classifier>${javacpp.platform}</classifier>
                </dependency>
                <dependency>
                    <groupId>org.bytedeco</groupId>
                    <artifactId>cuda-redist-cusparse</artifactId>
                    <version>${kompile.cuda.redist.version}</version>
                    <classifier>${javacpp.platform}</classifier>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
