<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
                              http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>native-dist</id>
    <formats>
        <format>tar.gz</format>
        <format>dir</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>
    <baseDirectory>kompile-app</baseDirectory>

    <fileSets>
        <!-- Native executables -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>kompile-app</include>
                <include>kompile-ingest</include>
                <include>kompile-vector</include>
                <include>kompile-embedding</include>
                <include>kompile-model-init</include>
                <include>kompile-vlm-test</include>
                <include>kompile-training</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Launcher script -->
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>*.sh</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>

        <!-- Application configuration -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources</directory>
            <outputDirectory>conf</outputDirectory>
            <includes>
                <include>application.properties</include>
                <include>application-*.properties</include>
                <include>log4j2*.xml</include>
                <include>log4j2.component.properties</include>
            </includes>
        </fileSet>

        <!-- Static UI assets -->
        <fileSet>
            <directory>${project.build.outputDirectory}/static</directory>
            <outputDirectory>static</outputDirectory>
        </fileSet>

        <!-- Empty directories for runtime data -->
        <fileSet>
            <directory>${project.basedir}/src/main/assembly</directory>
            <outputDirectory>data</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/src/main/assembly</directory>
            <outputDirectory>logs</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/src/main/assembly</directory>
            <outputDirectory>models</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>
