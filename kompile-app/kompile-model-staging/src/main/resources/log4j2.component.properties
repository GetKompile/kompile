# Force Log4j2 API to use SLF4J bridge, bypassing ServiceLoader discovery.
# This is critical for GraalVM native images where ServiceLoader.load() via
# MethodHandles is not supported.
log4j2.loggerContextFactory=org.apache.logging.slf4j.SLF4JLoggerContextFactory
log4j2.StatusLogger.level=OFF
