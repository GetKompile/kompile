/*
 * Build-time TypeScript config for the admin console. See chat-app/tsconfig.app.json for why
 * shared sources are not listed in `include` while the shared ambient .d.ts files are.
 */
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../out-tsc/admin-app",
    "types": []
  },
  "files": [
    "src/main.ts",
    "../kompile-ui-shared/src/polyfills.ts"
  ],
  "include": [
    "src/**/*.d.ts",
    "src/app/**/*.ts",
    "../kompile-ui-shared/src/**/*.d.ts"
  ],
  "exclude": [
    "src/**/*.spec.ts"
  ]
}
